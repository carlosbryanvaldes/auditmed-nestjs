# .env para desarrollo LOCAL con SQLite (sin necesidad de MySQL)
# Copiar como .env antes de arrancar en local

DATABASE_URL="file:./dev.db"
JWT_SECRET="dev-secret-auditmed-local-32chars-ok"
JWT_EXPIRES_IN="8h"
PORT=3000
NODE_ENV=development
CORS_ORIGIN="http://localhost:5173"
